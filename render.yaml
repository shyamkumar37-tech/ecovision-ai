###############################################################################
# EcoVision AI — Render Blueprint (100% Free Plan)
# Docs: https://render.com/docs/blueprint-spec
#
# HOSTING ARCHITECTURE:
#   - Backend & Celery Worker: Render Web Service (Free Tier)
#   - Cache / Task Broker: Upstash Redis (Free Tier)
#   - Database: Supabase PostgreSQL (Free Tier)
#   - Frontend: Vercel Static Web App (Free Tier)
#
# ENVIRONMENT SETUP IN RENDER DASHBOARD:
#   - DATABASE_URL = postgresql://postgres:[PASSWORD]@db.[REF].supabase.co:5432/postgres
#   - REDIS_URL    = redis://default:[PASSWORD]@[HOST].upstash.io:6379
#   - SECRET_KEY   = [YOUR-64-CHAR-HEX-SECRET]
#   - OPENROUTER_API_KEY = [YOUR-OPENROUTER-KEY]
###############################################################################

services:
  # ---------------------------------------------------------------------------
  # Backend API & Celery Worker — FastAPI (Native Python - 100% Free Tier)
  # ---------------------------------------------------------------------------
  - type: web
    name: ecovision-backend
    runtime: python
    buildCommand: bash scripts/render-build.sh
    startCommand: cd backend && (celery -A app.core.celery_app worker --loglevel=info &) && uvicorn app.main:app --host 0.0.0.0 --port $PORT
    plan: free
    region: oregon
    branch: main
    autoDeploy: true
    healthCheckPath: /health

    envVars:
      - key: ENVIRONMENT
        value: production
      - key: DEBUG
        value: "false"
      - key: UPLOAD_DIR
        value: /tmp/uploads
      - key: REPORTS_DIR
        value: /tmp/reports
      - key: ALLOWED_ORIGINS
        value: https://ecovision-frontend.vercel.app
      - key: OPENROUTER_MODEL
        value: openrouter/free
